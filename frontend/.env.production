# Production environment variables
# The API URL will be dynamically determined based on the current hostname
# For external access, make sure to update this with your server's external IP
# Example: NEXT_PUBLIC_API_URL=http://YOUR_EXTERNAL_IP:3002

# Leave empty to use dynamic hostname detection
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=